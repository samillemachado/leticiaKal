import { Box } from "@mui/material";
import React from "react";

const Footer: React.FC = () => {
  return (
    <React.Fragment>
      <Box></Box>
    </React.Fragment>
  );
};

export default Footer;
